# memories

is just a visual register of any king of record, if a record is edited, there is a register of any single change which is the optimal for financial apps, ERP, etc

## installation

- requires `pnpm` installed
- repo cloned on your local
- `docker` installed and running
- `docker compose up` for database and its structure
- `pnpm run dev` for running the frontend and back
